DATABASE_URL=postgresql+psycopg2://expense:expense@localhost:5432/expense_tracker
REDIS_URL=redis://localhost:6379/0

# JWT signing key — replace for any real deployment
JWT_SECRET_KEY=dev-only-secret-change-me

# Optional: enables the LLM secondary anomaly signal (app/analytics/llm_signal.py).
# Leave unset to run with the rule-based detector only.
OPENAI_API_KEY=
